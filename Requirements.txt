# Core
streamlit>=1.35.0
groq>=0.9.0
openai-whisper>=20231117
python-dotenv>=1.0.0

# PyTorch (CPU-only — swap for CUDA build if GPU available)
torch>=2.1.0

# Optional: speaker diarization (requires HF_TOKEN env var + model access)
# pyannote.audio>=3.1.0

# Audio processing
ffmpeg-python>=0.2.0